import 'dart:ui';

import 'package:flutter/material.dart';

class LightColors {
  /// The core 'dark' color used for text, icons, etc on light backgrounds
  static const textDarkest = Color(0xFF2D3B45);

  static const textDark = Color(0xFF6B7780);

  static const textLightest = Color(0xFFFFFFFF);

  static const backgroundLightest = Color(0xFFFFFFFF);

  static const backgroundLight = Color(0xFFF5F5F5);

  static const defaultAccent = Color(0xFF0081BD);

  static const defaultButton = Color(0xFF007BC2);
}