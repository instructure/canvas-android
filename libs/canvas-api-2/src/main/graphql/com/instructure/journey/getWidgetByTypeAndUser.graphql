query GetWidgetByTypeAndUser(
    $userAccountId: String!
    $type: String!
) {
    widgetByTypeAndUser(
        userAccountId: $userAccountId
        type: $type
    ) {
        id
        type
        position
        createdAt
        updatedAt
    }
}