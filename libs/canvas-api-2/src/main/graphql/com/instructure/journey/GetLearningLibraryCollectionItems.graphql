query GetLearningLibraryCollectionItems(
  $cursor: String
  $limit: Int = 10
  $forward: Boolean = true
  $bookmarkedOnly: Boolean = false
  $searchTerm: String
  $types: [CollectionItemType!]
  $completedOnly: Boolean = false,
  $sortBy: CollectionItemSortOption,
) {
  learningLibraryCollectionItems(
    input: {
      cursor: $cursor
      limit: $limit
      forward: $forward
      bookmarkedOnly: $bookmarkedOnly
      searchTerm: $searchTerm
      types: $types
      completedOnly: $completedOnly
      sortBy: $sortBy
    }
  ) {
    items {
      id
      libraryId
      itemType
      displayOrder
      canvasCourse {
        courseId
        canvasUrl
        courseName
        courseImageUrl
        moduleCount
        moduleItemCount
        estimatedDurationMinutes
      }
      programId
      programCourseId
      createdAt
      updatedAt
      isBookmarked
      completionPercentage
      isEnrolledInCanvas
      canvasEnrollmentId
      canvasModuleId
      canvasModuleItemId
      canvasResourceId
    }
    pageInfo {
      nextCursor
      previousCursor
      hasNextPage
      hasPreviousPage
      totalCount
      pageCursors {
          cursor
          page
      }
    }
  }
}